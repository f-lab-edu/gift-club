<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.giftclub.mapper.ProductMapper">

    <insert id="insertProduct" parameterType="com.giftclub.product.Product">
        INSERT INTO product(product_id, product_name, product_price, product_information, store_id)
        VALUES (#{productId}, #{productName}, #{productPrice}, #{productInformation}, #{storeId})
    </insert>

</mapper>