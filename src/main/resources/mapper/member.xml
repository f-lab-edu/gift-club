<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.giftclub.mapper.MemberMapper">

    <insert id="insertMember">
    INSERT INTO member(member_email, member_name, member_password, member_birth)
    values (#{memberEmail}, #{memberName}, #{memberPassword}, now());
</insert>

    <select id="findByEmailAndPassword" parameterType="com.giftclub.member.Member"
            resultType="com.giftclub.member.Member">
      SELECT *  FROM member WHERE member_email = #{memberEmail};
    </select>

    <select id="findByEmail" resultType="boolean">
        SELECT IF(COUNT(*) = 1, 1, 0) FROM member WHERE member_email = #{memberEmail};
    </select>

</mapper>