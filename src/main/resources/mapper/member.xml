<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.giftclub.member.MemberMapper">
    <insert id="insertMember" parameterType="com.giftclub.member.Member" useGeneratedKeys="true"
            keyProperty="memberId">
    insert into member(member_email, member_name, member_password, member_birth)
    values (#{memberEmail}, #{memberName}, #{memberPassword}, #{memberBirth}  );
</insert>
    <select id="checkEmailExists" parameterType="com.giftclub.member.Member" resultType="int">
    select count(member_email) from member where member_email =#{memberEmail} ;
</select>
    <select id="getMemberByMemberEmail" parameterType="com.giftclub.member.Member"
            resultType="com.giftclub.member.Member">
    select  * from member where member_email =#{memberEmail} ;
</select>
</mapper>